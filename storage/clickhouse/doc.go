/* Package clickhouse provides ClickHouse storage

This storage uses workers to simultaneously encode and send more queries

Initial table structure is send as CREATE IF NOT EXISTS on each `.Start` */
package clickhouse
